{
  "name": "{{packageName}}",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "@anteros/core": "{{coreVersion}}"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
